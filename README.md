# Algorithm_Sorted_Squared_Array

```
def sortedSquaredArray(array):
    answer = []
	for i in array: 
		answer.append(i)
		# answer.append(pow(i, 2))
print(answer.sort)
    
sample array = [-3, 1, 2]
expected answer = [1, 4, 9]
O(n)+ O(n log(n))


let i = index[0]
let j = index[array.len - 1]
i+=1
break array.len = answer.len

[4, 1, 9]
```

```
def sortedSquaredArray(array):
    answer = []
	for i in array: 
		answer.append(i)
		# answer.append(pow(i, 2))
print(answer.sort)
    
sample array = [-3, 1, 2]
expected answer = [1, 4, 9]
O(n)+ O(n log(n))


let i = index[0]
let j = index[array.len - 1]
i+=1
break array.len = answer.len

[4, 1, 9]
```
